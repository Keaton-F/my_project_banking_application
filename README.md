# my_project_banking_application

## Описание

Простой проект для работы с банковскими картами и счетами. Программа умеет маскировать номера карты и счета, преобразовывать дату, фильтровать и сортировать операции. Программа может взаимодействовать с файлами csv и xlsx, а запись логов ведёт в json.

## Структура

- **main.py** — примеры использования функций
- **src/masks.py** — маскировка номеров
- **src/processing.py** — обработка данных
- **src/widget.py** — преобразование даты, определение типа номера
- **src/decorator.py** — декораторы
- **src/generators.py** — генераторы
- **src/external_api.py** — токен и конвертация валют с помощью внешнего API
- **src/readers.py** — чтение файлов .csv и .xlsx
- **src/utils.py** — чтение JSON-файлов
- **test/** — тесты

## Как запустить

1. Запустите модуль main.py
2. Выберите необходимый пункт в меню, и установите условия фильтрации
3. Посмотреть результат работы в консоли

## Пример функций

- Маскирует номер карты/счета.
- Переводит дату и время в обычный вид.
- Фильтрует список по статусу.
- Сортирует по дате.
- Фильтрует транзакции по валюте.
- Последовательно получает описания операций.
- Генерирует номера банковских карт в заданном диапазоне.

## Генераторы

- Фильтруют транзакции по валюте (filter_by_currency)
- Последовательно получают описания операций (transaction_descriptions)
- Генерируют номера банковских карт в заданном диапазоне (card_number_generator)

## Тестирование

Для запуска тестов перейдите в директорию `test` и выполните соответствующие тестовые скрипты.

Так как теперь проект покрыт тестами с помощью фреймворка **pytest**, понадобится его установка.
Чтобы установить pytest, используйте команды:

Установка через Poetry: ```poetry add --group dev pytest```

Через менеджер пакетов pip: ```python -m pip install pytest```

### Недавние изменения

- В модуле обработки данных добавлены ф-ции обработки регулярных выражений
- Теперь для запуска программы можно использовать файл main.py
- Обновлена папка data

В read_transactions_from_csv было установлено разделение по умолчанию ",". Чтобы можно было прочитать
файл с помощью программы, разделитель был изменён на ";".

## Автор

Keaton-F